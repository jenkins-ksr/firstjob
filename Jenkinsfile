pipeline {
  agent { label 'staging' }

  stages {
    stage('Build') {
      steps {
          echo "This is the Build Stage"
      } 

    stage('Test') {
      steps {
         echo "This is the test stage"
      }
    }
    }
  }
}
